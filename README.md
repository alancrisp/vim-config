Clone repo to `~/.vim`, initialise and update submodules.

```
git clone https://github.com/alancrisp/vim-config.git ~/.vim
cd ~/.vim
git submodule init
git submodule update
```

Add this line to `~/.vimrc`

```
runtime vimrc
```
